import { InputHTMLAttributes } from "react";
import styled from "styled-components";
import sharedStyles, { BaseProps } from "../sharedStyles";

const StyledInput = styled.input`
  ${sharedStyles}
`;

export default StyledInput
